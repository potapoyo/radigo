# タイムフリー録音の音飛び修正

修正元: `v0.12.0+pota_20260322` (`18b102a`)
修正版: `v0.12.0+pota_2026090801`

## 対象と原因

JP13 / TBS / `20260908010000`、2026年9月8日01:00〜03:00の
「JUNK 伊集院光 深夜の馬鹿力」で検証した。

旧版は要求時刻を15秒ずつ進め、返却された音声がその区間を埋めるか確認しなかった。
実配信では01:00:15の要求に00:59:54頃からの別の断片群が返る例があり、
同一URIが異なる時刻情報で再登場した。URLだけで重複除外し、ファイル名順で結合する方式では
必要な区間の取りこぼしや順序の誤りを見逃す。

加えて、HTTP 403・503や空応答を音声の取得成功と扱うこと、同名ファイルの上書きも
模擬応答で再現した。これらは今回の実配信調査での直接原因とは区別する。

## 修正

- HLSの実時刻と時間長を保持し、連続して取得できた終端から次の範囲を取得する。
- 欠落や取得失敗は待機を入れて再試行する。進展しない状態が8回続けば明示的に失敗する。
- ミリ秒丸めの許容は10ms。時間情報が欠ける場合や未対応の暗号化・バイト範囲形式は失敗させる。
- 同じ録音内でセッションIDを維持し、各取得要求で新たなプリロールを要求しない（`preroll=0`）。
- 異なる時刻にある同一URIを一律に除外せず、取得順の一意な連番で保存・結合する。
- HTTPエラー、空応答、HTML等、途中で切れたAAC/ID3を拒否する。並列取得は8、最大4回の試行とし、キャンセル・タイムアウトを扱う。
- 共有エラーフラグのデータ競合をなくす。FFmpegエラーを表示し、結合失敗時にその入力を削除しない。中間ファイルを適切に閉じる。

セッションIDの固定だけでは改善しなかった。配信側の広告処理等の内部仕様は断定していない。

## 修正前の実測

| 条件 | 窓の取得回数 | URL重複除外後の断片数 | EXTINF合計 |
| --- | ---: | ---: | ---: |
| 旧版・1回目 | 480 | 1134 | 5672.121秒 |
| 旧版・2回目 | 480 | 1127 | 5637.472秒 |
| 旧版・3回目 | 480 | 1143 | 5714.020秒 |
| 旧版でlsidだけ固定 | 480 | 1133 | 5665.206秒 |

約94〜95分相当しか残らなかった。これは配信リストの集計であり、ユーザーの既存ファイルを
実測した値ではない。同一URIの時刻が変化するため、秒単位の欠損総量はこの集計だけでは確定しない。
欠落した01:00:45.317開始の断片を、その時刻への再要求で回収できた。

## 修正後の実録音検証

本体と同じ取得・保存・結合処理で上記番組を録音した。

- 取得断片: **1440個**。番組終端まで時刻の連続性確認に成功。
- 全断片のHTTP応答とAACフレーム検査: 成功。
- 完成AACをFFmpeg 4.4の `-xerror` で全編デコード: 成功。
- デコード実時間: **7199.957333秒**（約2時間）。

この検証により、指定番組での取得段階の欠落が改善したことを確認した。
全局・全放送日の保証や、聴感による全編チェックを行ったという意味ではない。

## 自動テスト

- 欠落範囲の再要求、古いリストの再返却、持続する欠落での失敗、キャンセル。
- 時刻順での収集、別時刻の同一URIの保持、相対URI・時刻補間、未対応形式の拒否。
- HTTP 403/503、空応答、HTML、途中で切れたAACの拒否と再試行。
- 保存名の衝突回避と順序保持、複数AACフレーム・ID3処理。
- FFmpeg結合失敗時の入力保持、100ファイルを超える結合でのデータ保持。

通常検証: `go test -race ./...`、`go vet ./...`、`go build ./cmd/radigo`。
FFmpegテストにはPATH上のFFmpegが必要。既存のパッケージ初期化ではエリア判定通信が発生する。

実録音テスト（配信期間内、書込み可能な空の検証先とFFmpegが必要）:

```sh
RADIGO_VERIFY_AUDIO_DIR=/absolute/path/to/verification go test -v . -run '^TestTBSRecordingIntegration$' -count=1
```

実録音テストは完成音声のデコード時間も検査し、不足していれば失敗する。
音声や認証情報はリポジトリ・リリースに含めない。
